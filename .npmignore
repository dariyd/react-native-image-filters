# Exclude example app entirely from npm package
example/
assets/

# Lock files and package manager artifacts (development only)
yarn.lock
package-lock.json
pnpm-lock.yaml

# Common junk / logs
npm-debug.log*
yarn-error.log*
.DS_Store

# Tests and coverage (not published)
__tests__/
coverage/

# Editor / tooling configs (not needed by consumers)
.vscode/
.idea/

# Build caches
.turbo/

# Development configs (not needed by consumers)
.eslintrc.js
.prettierrc.js

# iOS/Android build outputs inside the library (defensive; also handled by package.json files field)
android/build/
ios/build/
ios/Pods/

# Git metadata (defensive)
.git/
.gitmodules
.gitignore
